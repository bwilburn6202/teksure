# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
coverage/

# TekSure Brain — generated knowledge base files (large)
public/brain/guides/
public/brain/knowledge-base.json

# Worktree artifacts
.claude/worktrees/
.claire/worktrees/
.claude/settings.local.json

# Vite HMR leftovers
vite.config.ts.timestamp-*.mjs

# Old/duplicate lockfile — bun.lock is authoritative
# bun.lockb  (keep both for now; revisit)

# macOS conflict copies (iCloud / file-sync duplicates — e.g. "file 2.ts")
*[[:space:]]2.*
*[[:space:]]3.*

# Env files (never commit)
.env
.env.*

# Junk
vite.config.ts.timestamp-*.mjs
*.stale.bak
newprobe.txt
tsc_check_local.json

# FUSE mount temp files / vitest timestamps
.fuse_hidden*
vitest.config.ts.timestamp-*

# Sandbox leftovers: this mount cannot unlink some files, so sessions rename them
# out of the way instead (see CLAUDE.md). Keep them out of the index.
*.stale-*
*.stale2-*
*.stale3-*
*.old-[0-9]*
dist.stale*
.git/*.lock.*
scripts/revert-require-swap.mjs
